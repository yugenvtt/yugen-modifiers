/**
 * @file build/link.ts
 * @description provides information about the sync process.
 **/

const link = ( ) => 
{
	console.log( '[yugen-modifiers] | symlinks are disabled in this environment.' );
	console.log( '[yugen-modifiers] | use vite.local.ts and FOUNDRY_OUT_DIR in .env for live sync.' );
};

link( );
